# Workspace Rules for ProjectTracker

## Git Guidelines
- **Automatic GitHub Push**: Always stage (`git add .`), commit with a descriptive commit message (`git commit -m "..."`), and push to GitHub (`git push origin main`) automatically after completing tasks or making code changes.
