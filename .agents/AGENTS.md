# Workspace Rules for ProjectTracker

## Git & Vercel Deployment Guidelines
- **Automatic GitHub Push & Vercel Deployment**: Always stage (`git add .`), commit with a descriptive commit message (`git commit -m "..."`), and push to GitHub (`git push origin main`) automatically after completing tasks or making code changes to trigger Vercel's automatic production deployment pipeline.
